# ==================================
# Student Scheduler Platform Configuration
# ==================================

# ============ API Gateway ============
# Port for API Gateway
PORT=3001

# Frontend URL for CORS
FRONTEND_URL=http://localhost:3000

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Redis Configuration (for caching and real-time features)
REDIS_URL=redis://localhost:6379
REDIS_PASSWORD=

# ============ Database Configuration ============
# MongoDB Atlas Connection String
# Format: mongodb+srv://<username>:<password>@<cluster-url>/<database-name>?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/student-scheduler?retryWrites=true&w=majority

# Alternative local MongoDB (for development)
# MONGODB_URI=mongodb://localhost:27017/student-scheduler

# ============ Service URLs ============
# Microservice endpoints (used by API Gateway)
USER_SERVICE_URL=http://localhost:3002
TIMETABLE_SERVICE_URL=http://localhost:3003
FRIENDS_SERVICE_URL=http://localhost:3004
LOCATION_SERVICE_URL=http://localhost:3005
EVENTS_SERVICE_URL=http://localhost:3006
NOTIFICATION_SERVICE_URL=http://localhost:3007

# ============ Email Configuration ============
# SMTP Configuration for email verification and notifications
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-specific-password

# Email settings
FROM_EMAIL=noreply@studentscheduler.com
FROM_NAME=Student Scheduler Platform

# ============ File Upload Configuration ============
# Cloudinary for image uploads (profile pictures)
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# Maximum file size for uploads (in bytes)
MAX_FILE_SIZE=5242880  # 5MB

# ============ University API Configuration ============
# University-specific API keys for timetable import
# Add your university's API configuration here
UNIVERSITY_API_KEY=your-university-api-key
UNIVERSITY_API_BASE_URL=https://api.your-university.edu

# ============ Google OAuth Configuration ============
# For university Google OAuth integration
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:3001/auth/google/callback

# ============ Security Configuration ============
# CORS allowed origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,https://your-frontend-domain.com

# Rate limiting
RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
RATE_LIMIT_MAX_REQUESTS=100
AUTH_RATE_LIMIT_MAX=5

# ============ Location Services ============
# Geolocation service API key (if using external service)
LOCATION_API_KEY=your-location-api-key

# Maximum distance for proximity alerts (in kilometers)
PROXIMITY_RADIUS_KM=1

# ============ Notification Configuration ============
# Push notification service (Firebase, etc.)
FIREBASE_SERVER_KEY=your-firebase-server-key
FIREBASE_SENDER_ID=your-firebase-sender-id

# ============ Environment ============
NODE_ENV=development
LOG_LEVEL=debug

# ============ External APIs ============
# For importing university events, club activities, etc.
EVENTS_API_KEY=your-events-api-key
CALENDAR_SYNC_ENABLED=true

# ============ Monitoring and Analytics ============
# Application monitoring (optional)
SENTRY_DSN=your-sentry-dsn
ANALYTICS_ENABLED=false

# ============ Development Settings ============
# Database seeding
SEED_DATABASE=false
SEED_USERS_COUNT=50

# Debug settings
DEBUG_MODE=true
VERBOSE_LOGGING=true
